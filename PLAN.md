# 英语新闻 Feed 推荐系统方案

## 1. 业务背景与约束
- **数据来源**：短期内缺乏真实用户行为，用户点击日志小时级同步，Mine 数据集可作为初始训练集。
- **产品目标**：为英语新闻内容构建个性化推荐 Feed，支持冷启动和后续在线学习。
- **技术限制**：框架尚未确定，需探索轻量易扩展方案；支持离线训练 + 近实时召回/排序；需考虑资源有限情况下的部署（单机或少量节点）。

## 2. 总体架构设计
```
+-------------------+         +------------------+
| 内容采集/处理层     |         | 行为采集与埋点系统 |
+---------+---------+         +---------+--------+
          |                             |
          v                             v
   +------+--------+            +-------+-------+
   | 数据湖 & 特征仓 |<----------| 日志清洗与聚合 |
   +------+--------+            +-------+-------+
          |                             |
          +-------------+---------------+
                        v
         +--------------+---------------+
         | 模型训练与评估平台            |
         +------+-----------------------+
                |
     +----------+-----------+
     | 离线召回 |  在线服务  |
     +-----+----+-----------+
           |                |
           v                v
   推荐召回服务        排序/重排服务
           \              /
            \            /
             +----------+
             | 前端/客户端 |
             +----------+
```

### 核心组件
1. **内容管理与特征管道**
   - 收集 Mine 数据集中的新闻内容，补充元数据（主题、发布时间、来源、关键字）。
   - 构建文本表示（TF-IDF、预训练语言模型 embeddings）。
   - 维护作者/频道等内容特征。

2. **用户画像与行为聚合**
   - 初期依赖内容相似性和热门度；通过小时级的点击日志批处理构建用户偏好画像。
   - 设计用户兴趣向量（基于最近点击的文章 embedding 平均、话题分布等）。

3. **召回层**
   - 基于内容的召回：使用向量检索（FAISS/ScaNN）实现相似文章召回。
   - 热门/新鲜度召回：保证冷启动用户体验。
   - 协同过滤召回：后续上线，基于稀疏点击构建 Item-Item 共现图。

4. **排序层**
   - 初期使用轻量模型（Logistic Regression / GBDT）结合内容、用户、上下文特征。
   - 长期目标：多任务学习（点击率/停留时长），探索深度模型（DIN、DIEN、Transformer-based）。

5. **在线服务**
   - 部署推荐服务 API（Flask/FastAPI + gRPC/REST）。
   - 缓存策略：热门结果缓存，用户短期历史缓存。
   - AB 测试与指标上报，监控模型表现。

6. **训练与评估**
   - 采用 Pipeline 方式：数据准备 → 特征工程 → 模型训练 → 线下评估（AUC、Recall@K、MRR） → 上线。
   - 模型管理：版本化（MLflow/自建），可回滚。

## 3. 数据策略
- **初始数据集**：Mine 新闻数据（需要确认 License）。构建训练样本（正负样本、曝光日志模拟）。
- **特征工程**：
  - 文本特征：词袋、TF-IDF、BERT embedding。
  - 内容属性：主题、发布时间、来源可信度。
  - 用户特征：偏好主题分布、活跃时间段、设备类型（如有）。
  - 上下文特征：推荐位置、时间。
- **数据存储**：
  - 原始数据：对象存储/文件系统（Parquet）。
  - 特征库：特征服务（Feast 或自建 Redis/ElasticSearch）。
  - Embedding 向量：向量数据库（FAISS、Milvus）。

## 4. 冷启动与数据稀缺应对
- 内容侧冷启动：通过内容 embedding、主题聚类，推荐相似文章。
- 用户冷启动：热门榜、新品榜、编辑精选；结合地理/时间上下文。
- 人工评估 + 小样本在线测试收集初始行为。
- 使用迁移学习（预训练模型）、自监督学习增强数据。

## 5. 技术选型建议
- **训练框架**：
  - 初期：PyTorch Lightning / TensorFlow + 自建脚本；对比 EasyRec 的可行性。
  - 也可考虑推荐专用框架（RecBole、FuxiCTR）来快速验证模型。
- **特征服务与向量检索**：
  - 向量检索：FAISS（离线）→ Milvus（在线）。
  - 特征存储：Feast 或 Redis。
- **工作流调度**：Airflow / Prefect / Dagster。
- **在线服务**：FastAPI + Uvicorn/Gunicorn。
- **监控**：Prometheus + Grafana；日志分析使用 ELK。

## 6. 项目里程碑
1. **阶段一：原型验证（0-4 周）**
   - 清洗 Mine 数据集，构建内容库。
   - 实现内容 embedding 与热门榜召回。
   - 构建基础排序模型（LR/GBDT）。
   - 搭建离线评估 pipeline。

2. **阶段二：上线 MVP（5-8 周）**
   - 构建在线推荐 API，接入前端。
   - 实现小时级日志回流与批量更新。
   - 支持 AB 测试与在线监控。

3. **阶段三：迭代优化（9-16 周）**
   - 引入多路召回（协同过滤、embedding 检索）。
   - 升级排序模型为深度模型（DIN/DSSM）。
   - 加入重排层与多目标优化。
   - 完善自动化训练、模型回归监控。

4. **阶段四：扩展与智能化（16 周+）**
   - 采用强化学习/多臂老虎机提升点击率。
   - 加入生成式个性化摘要、音频/视频多模态扩展。
   - 构建推荐解释性、敏感内容审核流程。

## 7. 研发协作与文档
- 使用 Git + CI/CD（GitHub Actions）管理代码。
- 建立编码规范（PEP8）、文档目录（docs/）。
- 定期评审模型效果与业务指标。

## 8. 风险与应对
- **数据不足**：利用公共数据、合成曝光记录；优化日志采集链路缩短延迟。
- **模型泛化差**：引入正则化、在线学习、热启动策略。
- **系统复杂度高**：模块化、容器化部署；优先搭建最小可行架构。
- **合规与隐私**：遵守数据使用许可，构建敏感信息过滤与访问控制。

## 9. 下一步行动
1. 评估 Mine 数据集许可证，完成数据入湖。
2. 确定框架选型（EasyRec vs PyTorch/RecBole），搭建基础训练脚本。
3. 制定日志回流方案，确保小时级同步可靠性。
4. 建立项目代码结构（src/, data/, docs/）。
5. 安排人力资源与迭代节奏，启动阶段一任务。

